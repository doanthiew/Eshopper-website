@model IEnumerable<Category>

@foreach (var item in Model)
{
	<div class="panel panel-default">
		<div class="panel-heading">
			<h4 class="panel-title"><a href="#">@item.CAT_Name</a></h4>
		</div>
	</div>
}